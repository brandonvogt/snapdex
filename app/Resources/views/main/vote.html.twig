{% extends 'base.html.twig' %}
{% block content %}
    <div class="row">
        <div class="col-md-3">
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
        </div>
    </div>

{%endblock%}